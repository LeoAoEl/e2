---
import { Image } from "astro:assets";
import stillWorkingInmage from "../../assets/working/StillWorking.webp";
import bgImage from "../../assets/working/eventosBG.webp";
---

<div
  class="relative min-h-screen w-full flex items-center justify-center bg-cover bg-center bg-no-repeat p-6"
  style={`background-image: url(${bgImage.src});`}
>
  <section class="container mx-auto relative z-10 my-20 md:my-0">
    <div
      class="flex flex-col-reverse md:flex-row items-center justify-center gap-10 md:gap-16"
    >
      <div class="w-full md:w-1/2 flex justify-center md:justify-end">
        <div
          class="max-w-md w-full aspect-square backdrop-blur-3xl bg-secondary/10 rounded-3xl p-6 md:p-8 flex items-center justify-center border border-white/20"
        >
          <Image
            src={stillWorkingInmage}
            alt="Trabajando en la sección"
            width={500}
            height={500}
            class="w-full h-full object-cover rounded-2xl shadow-[0_20px_40px_-10px_rgba(0,0,0,0.5)] transform transition-transform hover:scale-[1.02] duration-500"
          />
        </div>
      </div>

      <div class="w-full md:w-1/2 text-center md:text-left">
        <div class="max-w-lg mx-auto md:mx-0">
          <h1
            class="text-4xl md:text-5xl font-bold text-gray-900 mb-6 drop-shadow-sm"
          >
            Mantente en contacto, pronto habrá noticias
          </h1>

          <p class="text-xl text-gray-800 mb-8 leading-relaxed font-medium">
            Próximamente encontrarás información detallada sobre nuestros
            eventos.
          </p>

          <div
            class="flex flex-col sm:flex-row gap-4 justify-center md:justify-start"
          >
            <a
              href="/contacto"
              class="px-8 py-3 bg-primary text-white font-semibold rounded-xl hover:bg-secondary transition-all duration-300 shadow-lg hover:shadow-xl hover:-translate-y-1 text-center"
            >
              Contáctanos
            </a>
            <a
              href="/"
              class="px-8 py-3 bg-white/80 backdrop-blur-sm border border-gray-300 text-gray-800 font-semibold rounded-xl hover:bg-white transition-all duration-300 hover:shadow-md text-center"
            >
              Volver al inicio
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>
</div>
