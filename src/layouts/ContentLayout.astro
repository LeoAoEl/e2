---
import RootLayout from './RootLayout.astro';

interface Props {
  title?: string;
  description?: string;
}

const props = Astro.props;
---

<RootLayout {...props} showHeader={false}>
	<slot />
</RootLayout>
